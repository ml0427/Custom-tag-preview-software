# 專案交接與開發筆記 (Handover Notes)

這份文件用於記錄目前的系統架構、已完成進度與未來接手的開發重點，方便後續 AI 或是開發人員能夠無縫接軌。

## 1. 系統環境與技術棧
* **前端 (Client)**：
  - 目錄：`client/`
  - 框架：Vue 3 + Vite + TypeScript (由 Webview2 渲染)
* **後端 (Server)**：
  - 目錄：`server/`
  - 框架：Spring Boot 3.4.2 (Java 17)
  - 資料庫：SQLite (`server/comic.db`)
  - 啟動方式：`.\mvnw spring-boot:run`
* **實體存放區**：
  - `comic_storage/`：存放來源 ZIP 漫畫檔。
  - `comic_cache/`：存放封面快取圖片。

## 2. 已完成進度 (STAGE 1 ~ 7)
1. **環境建置**：完成基礎專案結構與 SQLite 連線設定。
2. **資料庫設計**：建立 `Comic`, `Tag` 多對多關聯模型。
3. **核心檔案處理**：實作 `ZipService` 提取圖片。
4. **掃描與快取**：實作手動觸發式掃描與非同步封面快取。
5. **後端 API 服務**：提供分頁、標籤過濾、重新命名與封面重取接口。
6. **一鍵啟動**：建立 `start.bat` 與 `啟動說明.md`。
7. **【今日重點】檔案總管介面與自動化分類**：
   - **UI 改版**：將原本的 Grid 改為「資料清單 + 右側預覽窗格」，並支援**可拖動分隔線 (Resizer)**。
   - **鍵盤導覽**：支援 `上下鍵` 選取、`Enter` 開啟詳情、`F2` 快速重新命名。
   - **檔案管理**：實作清單**行內編輯 (Inline Rename)**，支援檔名左側編輯按鈕。
   - **自動標籤 (Auto-Tagging)**：利用正則表達式自動從 `[...]` 或 `【...】` 提取作者，且支援 `xxx(oooo)` 複雜格式拆分。
   - **掃描同步補強**：實作了「掃描前先行清空資料庫與快取」的機制，解決舊有紀錄標籤不完全的問題。

## 3. 未來升級任務列表 (Future Upgrade Missions)

### **MISSION 3: 雙視角隔離與操作體驗重構 (UX/Arch Refactoring) [📝 NEW]**
*   **視角與導航隔離**：切換工作區與標籤管理區時須保留各自邏輯與還原狀態。工作區顯示該目錄，標籤區顯示該標籤。支援從側邊預覽列點擊標籤，直接跳轉進標籤管理器並套用該標籤篩選。
*   **死碼殘留清理**：清查 Rust 後端 (`models.rs`) 中未使用的 `Comic` 與 `TagRule` 結構。
*   **自適應雙層排序邏輯**：
    1.  **左側目錄樹**：永遠遵循「先按類型 (資料夾優先) -> 再依名稱」排列。
    2.  **右側主列表**：嚴格遵循使用者選定的排序條件 (如依日期)，打破類型限制，全域項目混合排列。
*   **右鍵選單重構與點擊優化**：
    *   雙擊 ZIP 改為系統預設行為 (直接開啟閱讀)。
    *   移除預覽面板多餘的「用預設程式開啟」按鈕。
    *   右鍵選單補回遺失的「修改檔名 (Rename)」功能，並將「詳情/編輯標籤」移進右鍵選單。

### **MISSION 5: 核心 bug 修復與穩定性** [PENDING]
*   **標籤更新無反應**：[BUG] 需修正 `api.getComic` 忽略 ID 的邏輯錯誤，並實作後端 `get_comic` 指令。

## 4. 程式碼審查紀錄（2026-04-24）

### 觀察到的三個問題

| 優先 | 位置 | 問題 | 修法 |
|------|------|------|------|
| 高 | `ComicGallery.vue:51-57` | source 模式的 tag 篩選在前端 `.filter()` 做，應在後端 SQL 處理 | sourcePath + tagId 同時傳後端，移除前端二次過濾 |
| 中 | `api.ts` + `ComicGallery.vue:229-230` | 統一型別 `Item` 已存在，但 template 仍用 `: any` 橋接舊 `Comic`/`Folder`，TypeScript 保護失效 | `FileExplorerTable` 直接吃 `Item[]`，移除 legacy 轉型 |
| 低 | `ComicGallery.vue:115` | `size=9999` 全量請求，目前 <2000 筆尚可，資料量大後會卡 | 虛擬捲動或分頁 + cursor-based 查詢（暫緩） |

---

## 5. 長期紀錄
所有細節已記錄於 `C:\AI紀錄\AI筆記.txt`。
- **ID**: `Project_CustomTagPreview_Status_20260423` (專案架構概覽更新)
- **ID**: `CustomTagPreview_UX_Architecture_Refactor_20260423` (雙視角隔離、排序重構、右鍵交互體驗優化與死碼清理)
- **ID**: `ComicManager_Rename_AutoTag_20260418`
- **ID**: `TagPreview_DetailTagUpdate_API_Broken_20260418` 
- **ID**: `TagPreview_Workspace_Sources_Design_20260418` 
- **ID**: `ComicManager_Tauri_Migration_Strategy`
